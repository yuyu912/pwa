<view class="page"><text class="heading">城市穿搭雷达</text><text class="desc">只看城市级匿名趋势，不申请 GPS，不展示街道和具体发布者。</text><button class="city" bindtap="chooseRegion">{{location ? location.cityName+' ›' : '手动选择城市'}}</button><text wx:if="{{error}}" class="error">{{error}}</text><view wx:if="{{trend}}" class="notice"><text>{{trend.sourceLabel}}</text><text wx:if="{{trend.status==='ready'}}">统计范围：最近 {{trend.range==='24h'?'24 小时':'7 天'}} · {{trend.authorCount}} 位独立发布者</text><text wx:else>当前仅 {{trend.authorCount}} 位独立发布者，少于 {{trend.minimumAuthors}} 人，暂不生成榜单。</text></view><block wx:if="{{trend.status==='ready'}}"><view class="card"><text class="title">热门颜色</text><text wx:for="{{trend.colors}}" wx:key="name">{{index+1}}. {{item.name}} · {{item.count}}</text></view><view class="card"><text class="title">热门品类</text><text wx:for="{{trend.categories}}" wx:key="name">{{index+1}}. {{item.name}} · {{item.count}}</text></view><view class="card"><text class="title">热门风格</text><text wx:for="{{trend.styles}}" wx:key="name">{{index+1}}. {{item.name}} · {{item.count}}</text></view></block><text class="footnote">小红书合作权限未接通时，本页只展示自有社区与明确授权的演示样本，不会冒充实时小红书数据。</text></view>
